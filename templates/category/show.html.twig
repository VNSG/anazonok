{% extends 'base.html.twig' %}

{% block title %}Univers
	{{ category.title }}
{% endblock %}

{% block body %}
	<h1>{{ category.title }}</h1>

	<a class="btn btn-primary sample">Sample</a>

		<div class="container"
			<div class="row wrap">
				{% for product in category.products %}

					{% include "_parts/categoryCard.html.twig" %}
				{% else %}
					Aucun produit dans l'Univers
					{{ category.title }}.
				{% endfor %}
			</div>
		</div>


{% endblock %}
